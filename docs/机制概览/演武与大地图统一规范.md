# 演武与大地图统一规范

> 本文档是**机制权威**：定义演武模式与战役（大地图）模式如何共享基本规则、保持独立存档。  
> 决策依据：`docs/归档/策划决策/阶段9-演武与大地图机制统一决策记录.md`  
> 所有数值从 `data/*.json` 读取，禁止硬编码。  
> **策划定稿声明（2026-09-17）**：见 §0。

---

## 0. 定稿声明（策划确认，2026-09-17 二次修正）

> **演武就是战役。**  
> **这不是两个模式**，而是战役中的战斗环节（战术演武）与战略大地图环节。  
> **基本规则同一套**（战斗/攻城/地形/移动/建筑战斗效果等，零差异）。  
> **演武结果自然就是战役结果，必须写入存档**（战役档 `SaveManager`）。  
> 文化、外交等机制演武侧可简化，且须独立，不得改写共享内核。

| 措辞 | 准确含义 |
|:---|:---|
| 「演武就是战役」 | 同一局战役；演武是战役里的仗，不是另一个游戏 |
| 「结果写入存档」 | 结算写回 `CityManager`（城市 HP/归属）与 `PassManager`（关隘 HP/归属），并触发战役自动存档 |
| 「独立演武存档」 | `SkirmishSaveManager` 仅作**局中快照/续玩**，**不替代**战役结果 |
| 「可简化文化/外交」 | 演武过程不跑扩散/外交 AI；不影响战斗规则与结算写回 |

---

## 1. 设计原则

### 1.1 关系总表

| 层 | 关系 |
|:---|:---|
| **基本规则** | 完全共享，零差异（§1.2） |
| **演武结算** | **写入战役**（城市 HP、占城、关隘 HP/归属）（§1.5） |
| **战役存档** | 结算后自动 `SaveManager` 槽；含演武导致的城池变化 |
| **演武快照档** | 可选，用于局中保存/读档，最终以写回战役为准 |
| **文化/外交/整轮经营** | 演武过程可简化（§1.3） |

### 1.2 基本规则：必须完全统一（零差异）

- 同一套**程序实现**（共享内核，禁止第二套 `compute_*`）
- 同一套**数据**（`data/*.json`）
- 同一套**公式与阶段顺序**

| 模块 | 统一要求 |
|:---|:---|
| 单位战斗 / 攻城 / 地形移动 / 建筑战斗效果 | 完全相同 |
| UnitState v3 / 战斗 AI 评分 / 建造 API | 完全相同 |

**禁止**：以「演武/战役」为由另写简化战斗公式。

### 1.3 演武过程允许简化（须独立）

| 系统 | 演武过程 |
|:---|:---|
| 文化 | 可不跑扩散；需要只读权威源 |
| 外交 | 可不跑 AI/条约；进场敌对由场景预设 |
| 完整经营回合 | 一仗之内不跑税收/人口/科技整轮 |

简化项不得污染共享战斗内核。

### 1.4 存档

| 项 | 说明 |
|:---|:---|
| **战役存档** `SaveManager` | **权威**。演武结算写入 CityManager 后自动存档 |
| **演武快照** `SkirmishSaveManager` | 局中进度；读档续打；**结束仍以写回战役为准** |

### 1.5 演武结算写回战役（必须）

| 结果 | 写入战役 |
|:---|:---|
| 墙 HP | `CityManager.set_wall_hp`（场景 `city_id` 绑定城） |
| 城体 HP | `CityManager.set_city_hp`（城市一份 HP，与墙 API 同步语义） |
| 关隘 HP/归属 | `PassManager`（`data/passes.json` 初始 + 占领持久化） |
| 占城 | 胜方 `change_ownership` 对方据点城 |
| 存档 | `SaveManager.save_to_slot(AUTO_SLOT)` |

- 无 `city_id` 的教学场景：可跳过城池写回（配置层），**不是**另套规则  
- Demo 任务步骤仍由 `DemoFlow` 标记；占城数据以本写回为准  

### 1.6 适配层边界

| 允许 | 不允许 |
|:---|:---|
| 地图范围、预检、UI | 另一套伤害/攻城/移动公式 |
| 教学场景跳过写回（配置） | 默认「演武结果不进战役」 |
| 演武局中快照 | 用快照档替代战役结果 |

---

## 2. 架构分层

```
┌──────────────────────────────────────────────────────┐
│  共享内核 scripts/systems/（基础玩法唯一实现）          │
│  · unit_state                 单位状态 schema        │
│  · combat_ctx_builder         攻/防修正组装          │
│  · combat_resolver            伤害/反击/克制          │
│  · movement_reach             可达/ZOC/射程          │
│  · siege_resolver + wall_combat_rules  攻城/墙城     │
│  · building_combat_effects    建筑战斗效果            │
│  · ai_combat_scoring          战斗目标评分            │
└──────────┬───────────────────────────┬───────────────┘
           │                           │
    ┌──────▼──────┐             ┌──────▼──────┐
    │ 大地图适配层  │             │ 演武适配层    │
    │ StrategicMap │             │ TacticalSkirmish│
    │ · 宣战门闩   │             │ · 场景预设敌对 │
    │ · 全图/UI    │             │ · 切片/UI     │
    │ · 写回城池   │             │ · Demo 写回   │
    └─────────────┘             └─────────────┘
```

**禁止**：在任一 Manager 内再写第二套 `compute_*` 伤害/攻城/移动公式。

---

## 3. UnitState（单位状态权威 schema）

### 3.1 字段

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | String | 场景内唯一 |
| `faction_id` | String | 归属势力 |
| `unit_type_id` | String | `units.json` |
| `q`, `r` | int | axial 坐标 |
| `col`, `row` | int | odd-R 偏移（可选，地图对齐） |
| `hp`, `max_hp` | int | 生命 |
| `mp`, `max_mp` | int | 行动力（演武旧名 `speed`/`mp_remaining` 仅为兼容别名） |
| `morale` | int | 0~130，四段式见《战斗系统》 |
| `count` | int | 编队人数；演武默认 1，规则与大地图同一 HP 池 |
| `acted` | bool | 本回合是否已行动 |
| `skills` | Array | 兵种/将领技能 |
| `is_supplied` | bool | 是否有补给 |
| `attacks_this_turn` | int | 本回合攻击次数 |
| `burn_turns` / `burn_damage` | int | 火攻 DOT |
| `stranded_turns` | int | 搁浅 |
| `flanking_penalty` | int | 夹击/包围惩罚 |

### 3.2 规则

1. 序列化与对外 API 一律 `mp` / `max_mp`（schema_version = 3）  
2. 旧档补默认字段，**不删除**未知字段  
3. `count > 1`：HP 池编队结算，**禁止**管线外另乘人数系数  

---

## 4. CtxBuilder（修正组装）

### 4.1 必须进入统一组装的来源

| 来源 | 攻 | 防 | 查询 API（只读） |
|:---|:---:|:---:|:---|
| 地形 | ✓ | ✓ | `terrain.json` atk_mod / def_mod |
| 科技 | ✓ | ✓ | `TechSystem` |
| 学派 | ✓ | ✓ | `SchoolManager` |
| 武大夫 | ✓ | ✓ | `MinisterManager` |
| 国家民心 | ✓ | — | `GameManager` |
| 粮荒 | ✓ | ✓ | `GameManager` |
| 单位被动技能 | ✓ | — | UnitState.skills |
| 火攻/伏击 | ✓ | — | 季节 + 地形 |
| 单位士气四段 | ✓ | ✓ | `CombatResolver` |
| 建筑战斗效果（城防等） | — | ✓ | `BuildingCombatEffects` |
| 文化 mismatch（若启用） | — | ✓ | 只读 CityManager，经 def ctx |

### 4.2 规则

1. 大地图与演武必须调用**同一 CtxBuilder**  
2. 权威源在大地图/全局系统；演武只读，不另建快照公式  
3. 禁止 Manager 手拼第二套 ctx  
4. 生产路径必须传 ctx；空 ctx **仅**允许单元测试  

---

## 5. CombatPipeline（攻击阶段机）

### 5.1 阶段顺序（两边相同）

```
预检（适配层） → 伤害 → 反击 → 夹击/包围士气 → 火攻 DOT → 状态写回
```

| 阶段 | 内容 | 归属 |
|:---|:---|:---|
| **预检** | 同阵营/acted/距离/射程/MP | 适配层 |
| **宣战门闩** | 仅大地图流程需要 `are_at_war` | 适配层，**不进内核** |
| 伤害 | `CombatResolver.compute_damage` + ctx | 内核 |
| 反击/士气/火攻 | 见《战斗系统》 | 内核 |
| 写回 | hp/morale/burn/acted/mp | 适配层 |

### 5.2 输出结构

```
{ ok, reason, damage, counter_damage?, fire?, morale_delta_atk?, morale_delta_def? }
```

---

## 6. MovementReach（可达格）

两边共用，因素**完全一致**：

| 因素 | 说明 |
|:---|:---|
| 地形 `move_cost` | 天堑未解锁为不可达 |
| 骑兵/水军限制 | `cavalry_allowed` / `is_navigable` |
| ZOC | 敌方控制区额外消耗（同一 `zoc.*` 参数） |
| 远程遮挡 | 高程差减射程（同一 `MovementReach.effective_range*`） |
| 城墙挡格 | 未破墙不可通行/停留 |
| 搁浅 | 水域规则 |
| MP | 本回合剩余行动力（UnitState.mp） |

地图边界由适配层裁剪；**不得**用另一套移耗表。

---

## 7. 攻城与城墙（基础玩法，必须相同）

### 7.1 统一规则

| 规则 | 实现 | 数据 |
|:---|:---|:---|
| 器械倍率 | `WallCombatRules.siege_multiplier` | `city_combat.siege_damage_multiplier` |
| 墙/城伤害分流 | `WallCombatRules.split_damage_to_wall` | `city_combat.damage_split_wall` + `wall_struct_def_by_level` |
| 城防加成 | `WallCombatRules` / `BuildingCombatEffects.city_building_def_buff` | 墙 HP 比例 + 建筑 `defense_bonus` |
| 墙结构 HP | `buildings.json` wall `structure_hp` | 同一建筑表 |
| 箭塔 | `arrow_tower.tower_attack` | 同一建筑表 |
| 占城 | 墙/城 HP 归零 + 适配层 `CityManager.change_ownership` | 同一 CityManager API |

### 7.2 运行时状态：统一语义，允许「同一数据的场景缓存」

| 项 | 要求 |
|:---|:---|
| **公式与字段语义** | 两边完全一致（structure_hp、defense_bonus、分流比例） |
| **权威存储** | `CityManager` 城池建筑（入主存档） |
| **演武场景** | 若场景绑定 `city_id`，建筑效果**从 CityManager 读取**；战斗中墙损写在场景缓存 |
| **写回** | 属 §1.2 写回策略；**不**因写回与否而改变攻城公式 |

> 「场景缓存」≠「另一套机制」。审核标准是：**同一公式、同一建筑数据语义、同一 JSON 系数**。

### 7.3 中立城

- 大地图：玩家可攻中立；AI 默认不攻中立（战略策略）  
- 演武：按场景配置单位与敌对；**若发生攻城，公式与大地图相同**

---

## 8. AI（战斗向统一；战略流程可分层）

| 层 | 要求 |
|:---|:---|
| **战斗目标评分** | 必须用 `AiCombatScoring`（残血、近距、器械对城） |
| **tutorial 档** | 仅削弱「追杀强度/引导」，**不得**改伤害/移耗/墙城公式 |
| **random 档** | 仅测试兼容；不得作为正式内容默认 |
| **大地图战略 AI** | 额外约束：宣战过滤、每回合行动预算——属流程，不属战斗公式 |

---

## 9. 文化与外交（允许简化，且独立）

| 系统 | 演武行为 | 独立性 |
|:---|:---|:---|
| 文化扩散/主流变更 | **不跑** | 不进移动/伤害第二套逻辑 |
| 文化 mismatch | 只读权威源；进战斗只经 def ctx | 经 CtxBuilder，不旁路 |
| 外交 AI/条约 | **不跑** | 不进内核 |
| 宣战检查 | 场景预设敌对 | 适配层预检 |
| 科技/学派/大夫/粮荒 | CtxBuilder **实时只读** | 与大地图同一数据源 |
| Demo 占城写回 | 可选策略 | 不改公式 |

---

## 10. 建造 API（经营入口，统一）

```
can_build(city_id, building_id, axial = Vector2i(-9999,-9999))
start_build(city_id, building_id, axial = Vector2i(-9999,-9999))
get_jurisdiction_hexes(city_id)  # 视觉辖区环，轴向
```

- 城池面板、大地图放置、演武内放置、EconomyAI **同一入口**  
- 辖区、占用、reason 契约见《城市经营》  

---

## 11. 审核清单（对照本原则）

### 11.1 已符合（程序现状）

| 项 | 证据 |
|:---|:---|
| 战斗公式 | `combat_resolver.gd`，两边调用 |
| Ctx 组装 | `combat_ctx_builder.gd` |
| 移动/ZOC/射程 | `movement_reach.gd`（战略 + 演武） |
| 攻城/墙城公式 | `siege_resolver.gd` + `wall_combat_rules.gd` |
| 建筑战斗效果 | `building_combat_effects.gd`（墙/城防/箭塔） |
| UnitState | `unit_state.gd` |
| AI 战斗评分 | `ai_combat_scoring.gd` |
| 建造 API | CityManager 三参 |

### 11.2 审核结论（2026-09-17 总体收口）

| # | 项 | 要求 | 状态 |
|:---|:---|:---|:---|
| G1 | 演武结果写入战役 | **演武=战役**：墙/城体/占城写回 CityManager + 战役自动存档 | ✅ `apply_result_to_campaign` |
| G2 | UnitState / 伤害池 | 同一 schema 与规则 | ✅ |
| G3 | 战斗 AI 评分 | 同一 `AiCombatScoring` | ✅ |
| G4 | 场景 `ai_mode` | 默认 scored；教学 tutorial；random 仅显式 | ✅ JSON + `_resolve_ai_mode` |
| G5 | 技能移耗叠加 | 叠在同一 MovementReach 结果上 | ✅ |
| G6 | 文化/外交/整轮经营 | 演武过程可简化，独立 | ✅ 按设计 |
| G7 | 演武存档 UI | Demo/演武「存档/读档」按钮 | ✅ 槽1：演武快照 + 战役档 |
| G8 | 基本规则 | 战斗/攻城/地形/移动/建筑效果零差异 | ✅ 共享内核 |

### 11.3 文档与机制权威

- 战斗数值：`docs/机制概览/战斗系统.md`  
- 地形/ZOC：`docs/机制概览/地理与天堑系统.md`  
- 辖区/建筑：`docs/机制概览/城市经营系统.md`  
- 统一层：**本文档**  

---

## 12. 程序实现状态

| 模块 | 状态 | 说明 |
|:---|:---|:---|
| CombatResolver | ✅ | 共享 |
| CtxBuilder | ✅ | 战略 + **演武攻击/反击管线**基础修正 |
| MovementReach | ✅ | 战略 + 演武 |
| Siege + WallCombatRules | ✅ | 共享 |
| BuildingCombatEffects | ✅ | 演武建筑战斗效果 |
| UnitState v3 | ✅ | 演武 spawn 经 `UnitState.make`；`mp` 权威 + `mp_remaining` 别名 |
| AiCombatScoring | ✅ | 默认 scored；tutorial/random 由场景 `ai_mode` |
| 演武结算写回战役 | ✅ | 墙/城体/占城 + 战役自动存档 |
| 演武局中快照档 | ✅ | `SkirmishSaveManager` |
| 建造 API | ✅ | CityManager |
| 演武存档 UI | ✅ | 存档/读档（快照 + 战役槽1） |
| 文化/外交简化 | ✅ | 演武过程只读/不跑 |

---

## 13. 相关文档

- 《战斗系统》《地理与天堑系统》《城市经营系统》  
- 阶段 9 决策记录（原则：演武就是战役）  
- `docs/程序进度/程序已实现功能.md`
