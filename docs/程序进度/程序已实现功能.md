# 《山河策》程序已实现功能

> **用途**：记录当前工程已落地的代码模块，供策划/程序对齐进度。
> **最后更新**：2026-05-11
> **代码总量**：~6500 行 GDScript（14 源文件 + 5 UI 场景 + 6 测试文件）

---

## 一、架构总览

### 1.1 Autoload 单例（8 个）

| 单例 | 文件 | 行数 | 职责 |
|---|---|---|---|
| SignalBus | `signal_bus.gd` | 70 | 全局信号总线（27 信号） |
| DataManager | `data_manager.gd` | 450 | JSON 数据加载、索引、校验 |
| GameManager | `game_manager.gd` | 377 | 游戏状态机、资源管理、回合循环 |
| CityManager | `city_manager.gd` | 505 | 城市运行时状态、建造系统、占领与迁都 |
| EventManager | `event_manager.gd` | 152 | 随机事件触发与结算 |
| DiplomacySystem | `diplomacy_system.gd` | 676 | 外交关系与行动（好感度/声望/条约/附庸） |
| TechSystem | `tech_system.gd` | 446 | 科技树（54 项、4 类×3 时代、27 种效果） |
| TacticalSkirmishManager | `tactical_skirmish_manager.gd` | 474 | 阶段1 战术演武（六角移动+战斗+占城） |

### 1.2 独立系统库

| 模块 | 文件 | 行数 | 职责 |
|---|---|---|---|
| 六角网格工具 | `hex_axial.gd` | 95 | 轴向坐标、odd-R 互转、距离、邻居、像素摆放 |
| 战斗结算 | `combat_resolver.gd` | 65 | 伤害计算（攻防/地形/士气/克制/伏击/随机波动） |
| 外交 AI | `diplomacy_ai.gd` | 338 | AI 外交决策（战争/停战/同盟/附庸/联合抗敌） |

### 1.3 UI 场景

| 场景 | 文件 | 行数 | 对应系统 |
|---|---|---|---|
| 主场景 | `main.gd` + `main.tscn` | 42 | 入口，挂载各系统面板 |
| 外交面板 | `diplomacy_panel.gd` + `.tscn` | 300 | 势力列表、操作按钮、详情 |
| 谈判对话框 | `negotiation_dialog.gd` + `.tscn` | 217 | 停战谈判（多轮反提议） |
| 科技面板 | `tech_tree_panel.gd` | 375 | 网格布局、详情、研究操作 |
| 战术演武面板 | `skirmish_mvp_panel.gd` + `.tscn` | 565 | 六角棋盘+移动+战斗 |
| 六角格控件 | `skirmish_hex_cell.gd` | 224 | 单格六角绘制、拾取、描边 |
| 六角画布 | `skirmish_hex_map_canvas.gd` | 70 | 批量绘制地形贴图（防缝隙） |
| 贴图管理 | `skirmish_tile_textures.gd` | 69 | 地形/兵种/首都贴图缓存 |

---

## 二、各系统实现详情

### 2.1 数据管线 ✅

**文件**：`data_manager.gd`（450 行）

| 已实现 | 说明 |
|---|---|
| 11 份 JSON 加载 | terrain, units, buildings, cities, events, balance_params, wonders, factions, diplomacy, tech_tree, tactical_skirmish_mvp |
| 多维度索引 | 地形 ID→数据、兵种 ID→数据、城市 ID→状态、科技 ID→数据、势力 ID→数据 |
| 国家变体合并 | `get_faction_variant()` —— 基础兵种 + 国家特色覆盖 |
| 克制矩阵查询 | `get_counter_multiplier()` —— 8×8 矩阵 |
| 季节计算 | `get_current_season(turn_number)` —— 4 回合一年 |
| 平衡参数读取 | `get_balance_param("combat.min_damage")` 点号路径 |
| 数据校验 | `validate_data()` —— 启动时检查全部 JSON 的结构完整性 |

| 待实现 | 说明 |
|---|---|
| 学派数据接口 | `get_school()` 返回空字典 |
| 存档序列化 | JSON/二进制存档读写 |

---

### 2.2 游戏状态管理 ✅

**文件**：`game_manager.gd`（377 行）

| 已实现 | 说明 |
|---|---|
| 阶段状态机 | 开局→回合开始→行动中→回合结束 |
| 8 种资源追踪 | food / gold / wood / craftsmen / building_materials / morale / population / troops |
| 资源增减 | 6 个 `apply_*_delta()` 方法，均含 clamp |
| 难度系统 | easy/normal/hard/hell 四档 |
| 回合数递增 | `advance_turn()` |
| 势力轮流 | 多势力按序行动 |
| 胜利判定 | 与 CityManager 配合的征服胜负 |
| 信号发射 | turn_started / turn_ended / phase_changed / game_over |
| 测试覆盖 | 6 个单元测试 |

| 待实现 | 说明 |
|---|---|
| 每回合资源产出结算 | 建筑效果/季节修正/税率均未接入 |
| 厌战情绪 | 战争超 20 回合 -2 民心/回合 |

---

### 2.3 城市管理系统 🔶

**文件**：`city_manager.gd`（505 行）

| 已实现 | 说明 |
|---|---|
| 50 城运行时初始化 | 从 `cities.json` 读取全部城市数据 |
| 势力城市索引 | `get_faction_city_states(faction_id)` |
| 建造验证（7 项） | 城市存在→势力归属→槽位→资源→限建数量→前置建筑→重复建造 |
| 建造队列 | `start_build()` —— 扣除资源、入队列 |
| 拆除建筑 | `demolish_building()` |
| 建筑升级 | `upgrade_building()` |
| 城市占领 | `occupy_city()` —— 易主、清空队列、安定度惩罚 |
| 迁都系统 | `relocate_capital()` —— 主动迁都 + 被迫迁都（首都沦陷），最多 2 次 |
| 灭国判定 | 势力无城市时判定灭亡 |
| 测试覆盖 | 16 个单元测试 |

| 待实现 | 说明 |
|---|---|
| 建造队列回合递减 | `turns_remaining` 未每回合 -1 |
| 建筑效果生效 | effects（粮食/金钱/城防/征兵加成）未接入 GameManager |
| 人口增长/衰减 | `current_population` 初始化后不变 |
| 城市防御/驻军 | wall、barracks 效果未使用 |
| 辖区辐射 | `jurisdiction_radius` 字段未使用 |

---

### 2.4 战术演武 MVP ✅

**文件**：`tactical_skirmish_manager.gd`（474 行）+ 4 个 UI 文件 + 1 个 shader

| 已实现 | 说明 |
|---|---|
| 独立六角棋盘 | 7×7 odd-R 偏移格，JSON 可配置任意尺寸 |
| 地形贴图渲染 | 7 种地形纹理 + 平顶六角密铺 + bleed 防缝隙 |
| 轴向坐标系统 | odd-R ↔ 轴向互转、六邻方向、hex 距离 |
| Dijkstra 寻路 | 按地形移耗，考虑骑兵/水军限制、友军阻挡、不可通行格 |
| 战斗结算 | 兵种攻防 + 地形修正 + 士气修正 + 克制矩阵 + 随机波动 ±10% + 伏击 |
| 攻击消耗移动力 | `balance_params.json` → `tactical.attack_move_cost` |
| 移动后可攻击 | MP 足够且有射程内敌军时可继续攻击 |
| 胜利条件 | 单位进入对方城格即胜 |
| AI 回合 | 随机可行走步 + 射程内随机攻击 |
| 首都贴图 | 秦/赵首都美术叠在地形上 |
| 兵牌显示 | 兵种图标 + 势力标记 + HP |
| 悬停信息 | 鼠标悬停显示地形效果、单位详情 |
| 颜色叠层 | 选中 > 可攻击 > 可移动 > 空城/势力占位 |
| shader | 六角地形顶光+边缘冷色效果 |
| 测试覆盖 | 六角 8 用例 + 战斗 5 用例 + 管理器 5 用例 |

---

### 2.5 外交系统 ✅

**文件**：`diplomacy_system.gd`（676 行）+ `diplomacy_ai.gd`（338 行）

| 已实现 | 说明 |
|---|---|
| 双向好感度 | -100~100，每对势力独立 |
| 声望系统 | 0~100，5 个等级 |
| 宣战 | 破坏条约、全局好感惩罚、声望损失 |
| 停战谈判 | 金钱/城市/附庸条件，AI 多轮反提议（最多 3 轮） |
| 互不侵犯条约 | 有时限 |
| 同盟 | 好感度 30+ 门槛，背叛后果 |
| 军事通行权 | 按部队收费，有时限 |
| 商路 | 基础收入 + 声望加成 |
| 赠礼 | 3 档：50/150/300 金 |
| 附庸系统 | 15% 纳贡，3 种脱离方式 |
| 中立城吞并 | 金 cost + 好感下降 |
| 关隘开放 | 多因素评分公式 |
| 好感度衰减 | 每 5 回合向 0 回归 1 点 |
| 条约到期 | 回合倒计时 |
| 战争冷却 | 5 回合 |
| AI 决策 | 战争目标/停战/同盟/联合抗敌/附庸脱离 |

| 待实现 | 说明 |
|---|---|
| 建筑外交效果 | `_apply_building_diplomacy_effects()` 为空函数 |
| 势力评分完整 | `get_power_score()` 中 culture/tech 硬编码为 0 |

---

### 2.6 科技树系统 ✅

**文件**：`tech_system.gd`（446 行）

| 已实现 | 说明 |
|---|---|
| 54 项科技 | 4 类×3 时代 |
| 前置链验证 | 递归检查所有前置科技 |
| 特殊条件框架 | city_control / reputation / building / region_control |
| 研究进度 | cost_turns = ⌈cost_gold/100⌉ × 速度修正 |
| 27 种效果类型 | attack_bonus, defense_bonus, unlock_unit, resource_bonus 等 |
| 18 个查询接口 | 供其他系统调用 |
| AI 研究 | 简化：每回合直接完成 |
| UI | 网格布局+颜色编码+详情面板 |

| 待实现 | 说明 |
|---|---|
| 建筑条件检查 | `_has_building()` 返回 false（未连接 CityManager） |
| 区域控制检查 | `_control_region()` 返回 false |
| 声望条件 | `fame` 特殊条件为 `pass` |

---

### 2.7 随机事件系统 ✅

**文件**：`event_manager.gd`（152 行）

| 已实现 | 说明 |
|---|---|
| 54 个事件 | 8 类（经济/军事/民心/季节/政治/外交/特殊/学派） |
| 条件过滤 | season / morale / turn / faction / war / tech |
| 概率触发 | `randf() <= probability` |
| 冷却系统 | 3 回合默认冷却 |
| 双路径 | 无选项直接生效 / 有选项发出 signal |
| 效果应用 | food/gold/wood/craftsmen/building_materials/morale/population/troops 增减 |

| 待实现 | 说明 |
|---|---|
| 事件 UI 面板 | `event_triggered` 信号无 UI 监听 |
| AI 事件 | 仅触发玩家势力 |
| 事件链 | 一事件触发另一事件 |

---

### 2.8 六角网格 + 战斗结算（大地图层）🔶

**文件**：`hex_axial.gd`（95 行）+ `combat_resolver.gd`（65 行）

| 已实现 | 说明 |
|---|---|
| 轴向坐标算法 | odd-R ↔ 轴向互转、六邻方向、hex 距离、矩形范围迭代 |
| 两种像素摆放 | 点状顶（校验用）+ 平顶（战术盘面） |
| 基础伤害计算 | 攻击力×修正 − 防御力×修正，保底 1 点 |
| 士气修正 | `morale_attack_multiplier()` 线性插值 |
| 克制矩阵应用 | `get_counter_multiplier()` 8×8 |
| 地形修正 | atk_mod / def_mod / ambush_chance |
| 伏击 | 基础 5% + 地形加成，伤害×2 |
| 随机波动 | ±10%，RNG 可复现 |

| 待实现 | 说明 |
|---|---|
| 大地图 30×20 TileMap | `cities.json` → hex_q/hex_r 有数据，无运行时地图 |
| A* 寻路 | 仅战术演武有 Dijkstra，大地图层无 |
| 天堑通过性 | `is_barrier` / `crossing_rules` 数据存在，无运行时强制 |
| 夹击/火攻/断粮 | 公式和参数已定义，无结算代码 |
| 关隘战斗规则 | 数据定义存在，未接入结算 |
| 将军系统 | 参数已定义，无代码 |

---

## 三、测试覆盖

| 测试文件 | 用例数 | 覆盖系统 |
|---|---|---|
| `test_city_manager.gd`（571 行） | 16 | 城市初始化/建造/拆除/升级/占领/迁都/灭国/限建 |
| `test_combat_resolver.gd`（73 行） | 5 | 伤害计算/士气/克制/伏击/地形 |
| `test_data_manager.gd`（94 行） | 8 | 数据加载/索引/校验 |
| `test_game_manager.gd`（112 行） | 6 | 资源管理/回合/难度 |
| `test_hex_axial.gd`（67 行） | 8 | 坐标互转/邻居/距离/像素摆放 |
| `test_tactical_skirmish_manager.gd`（33 行） | 5 | 演武启动/寻路/攻击消耗/贴图 |

**总计**：~48 个测试用例，覆盖 6 个核心模块。

---

## 四、数据文件

| 文件 | 记录数 | 加载状态 |
|---|---|---|
| `terrain.json` | 7 种地形 | ✅ |
| `units.json` | 8 基础兵种 + 7 国家变体 | ✅ |
| `buildings.json` | 10 种建筑 | ✅ |
| `cities.json` | 50 座城市 | ✅ |
| `events.json` | 54 个事件 | ✅ |
| `balance_params.json` | 全局参数 | ✅ |
| `wonders.json` | 5 座奇观 | ✅ |
| `factions.json` | 7 个势力 | ✅ |
| `diplomacy.json` | 外交参数 | ✅ |
| `tech_tree.json` | 54 项科技 | ✅ |
| `tactical_skirmish_mvp.json` | 7×7 战术地图配置 | ✅ |
| `schools.json` | 6 个学派 | ✅ 加载但无代码使用 |

---

## 五、与策划案阶段对应

| 策划案阶段 | 程序完成度 | 备注 |
|---|---|---|
| **阶段0 地基** | ✅ 100% | DataManager、核心公式、六角网格坐标 |
| **阶段1 原型** | 🔶 ~60% | 战术演武可玩（独立棋盘），大地图闭环未打通 |
| **阶段2 外交+科技** | ✅ ~90% | 外交完整、科技完整，少量条件函数占位 |
| **阶段3 天堑+文化** | 🔶 ~20% | 迁都提前完成；天堑/民心/学派/文化均仅数据 |
| **阶段4 经营+将军** | ⏳ ~5% | 数据定义存在，代码基本为零 |
| **阶段5 打磨** | ⏳ 0% | 存档/教程/性能优化未开始 |

---

## 六、维护说明

- 本文随代码提交更新；重大模块上线后请同步修订对应章节。
- 技术接口细节以 `docs/接口文档.md`、代码注释及 `CLAUDE.md` 为准。
