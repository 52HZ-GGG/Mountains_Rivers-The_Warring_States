# 《山河策》阶段 5 战斗系统增强决策记录

> **状态**：✅ 已定稿（2026-05-13）
> **来源**：战斗系统完善，基于用户需求与游戏机制总览
> **依赖**：阶段0-决策记录.md、阶段4-城市模块决策记录.md

---

## 本次产出物

| 文件 | 说明 |
|:---|:---|
| `data/terrain.json` | 修改：ford（渡口）的 `is_navigable` 从 `false` 改为 `true` |
| `data/factions.json` | 新增：七国 `action_speed` 字段（3-5） |
| `docs/机制概览/战斗系统.md` | 更新：§8.2 水军移动规则、新增 §10 战斗状态机与回合机制 |
| `scripts/systems/combat_resolver.gd` | 更新：添加伤害溢出机制文档说明 |
| `docs/策划决策/阶段5-战斗系统增强决策记录.md` | 本文件 |

---

## 关键决策

### 决策 126：水军可移动地形 — 河流 + 渡口

**选定方案**：
- 水军可移动单元格定义为：河流（river）、渡口（ford）两种地形
- 渡口的 `is_navigable` 字段从 `false` 改为 `true`
- 在水面单元格修建建筑后，水军依然可以正常通行

**理由**：
- 渡口作为河流的浅水区域，历史上是水军重要的停靠和补给点
- 水军需要更多可通行地形来增加战术灵活性
- 建筑不阻碍水军通行符合物理逻辑（码头、港口等建筑本就是为水军建造的）

**已落地**：
- `data/terrain.json` → ford 条目：`is_navigable: true`
- `docs/机制概览/战斗系统.md` → §8.2 水军移动规则

---

### 决策 127：行动速度与回合顺序机制

**选定方案**：
- 在 `factions.json` 中为七国定义 `action_speed` 字段（范围 3-5）
- 每回合开始时按 `action_speed` 降序排列，决定行动顺序
- 每回合内**只有一个势力**可以执行所有操作（移动、攻击、经营、外交）
- 相同 `action_speed` 的势力随机排序（游戏开始时固定种子）

**行动速度分配**：

| 势力 | action_speed | 设计理由 |
|:---|:---:|:---|
| 秦 | 80 | 虎狼之师，商鞅变法后军纪严明，行动迅速 |
| 赵 | 70 | 胡服骑射改革，骑兵机动性强 |
| 魏 | 65 | 武卒精锐，训练有素 |
| 燕 | 60 | 坚韧不拔，执行力强 |
| 齐 | 55 | 文风鼎盛，节奏稳健 |
| 楚 | 50 | 地广人众，调度较慢 |
| 韩 | 45 | 精工巧匠，稳扎稳打 |

**理由**：
- 行动速度机制创造先后手策略差异，高速势力可先发制人
- 单势力行动回合制简化状态管理，避免多势力同时操作的复杂性
- 数值设计体现历史特征（秦赵尚武，齐楚重文/地广）

**已落地**：
- `data/factions.json` → 各势力新增 `action_speed` 字段
- `docs/机制概览/战斗系统.md` → 新增 §10 战斗状态机与回合机制

---

### 决策 128：单格单部队限制

**选定方案**：
- 每个六角格只能容纳一支部队
- 已被友军占据的格子无法进入
- 已被敌军占据的格子需要先击败守军才能占领
- 移动路径规划时自动绕过已占据格子

**理由**：
- 单格单部队简化战斗结算逻辑
- 避免堆叠导致的数值爆炸问题
- 促使玩家合理规划部队阵型和进攻路线

**已落地**：
- `scripts/autoload/tactical_skirmish_manager.gd` → `_occupant_id_at()` 检查
- `docs/机制概览/战斗系统.md` → §10.3 单格单部队限制

---

### 决策 129：伤害溢出机制

**选定方案**：
- 当部队被击杀时，多余的伤害直接溢出，不保留
- 溢出伤害不转移至其他单位
- 公式：`实际伤害 = min(攻击力, 目标当前HP)`

**示例**：
- 攻击力 50，目标 HP 30 → 造成 30 伤害，目标被歼灭，20 点溢出伤害丢弃
- 攻击力 50，目标 HP 80 → 造成 50 伤害，目标剩余 30 HP

**理由**：
- 简化战斗结算，避免复杂的伤害转移逻辑
- 符合直觉（击杀后多余的动能消散）
- 防止通过伤害转移实现过于强力的连锁击杀

**已落地**：
- `scripts/systems/combat_resolver.gd` → 添加溢出机制文档说明
- `scripts/autoload/tactical_skirmish_manager.gd` → 现有代码已实现此行为
- `docs/机制概览/战斗系统.md` → §10.4 伤害溢出机制

---

## 状态机设计

### 回合状态枚举

```gdscript
enum TurnState {
    TURN_START,        # 回合开始，计算行动顺序
    FACTION_ACTION,    # 势力行动中
    FACTION_END,       # 势力结束回合
    TURN_END           # 回合结束，触发回合结束效果
}
```

### 回合流程

```
1. TURN_START → 按 action_speed 排序确定行动顺序
2. FACTION_ACTION → 当前势力执行所有操作
3. FACTION_END → 当前势力结束回合
4. 下一个势力 → 回到 FACTION_ACTION
5. 所有势力完毕 → TURN_END → 触发回合结束效果
6. 回到 TURN_START
```

---

### 决策 130：区域控制（ZoC）机制

**选定方案**：
- 步兵类和骑兵类单位对相邻六角格施加 ZoC（弓兵、攻城、水军不施加）
- 所有单位受 ZoC 约束，拥有 `recon` 标签的斥候类单位免疫
- 效果：处于敌方 ZoC 中的单位移动力 ×0.5（独立乘算）
- 离开 ZoC 格子额外消耗 1 MP
- ZoC 不阻止移动，仅增加移动代价
- 撤退/溃退中的单位不受 ZoC 约束

**理由**：
- ZoC 是战棋游戏的核心机制，阻止敌方单位自由穿越前线
- 步兵/骑兵施加 ZoC 体现其"控制战场"的战术角色
- 斥候免疫 ZoC 保持其侦察/渗透的定位

**已落地**：
- `docs/机制概览/战斗系统.md` → §5.7 区域控制、§9.4 区域控制对移动力的影响
- `docs/策划决策/阶段5-平衡性调整文档.md` → 决策 #161

---

### 决策 131：撤退/溃退与追击机制

**选定方案**：
- **主动撤退**：玩家可指令撤退，消耗全部行动，自动远离敌军，持续撤退至安全距离 ≥ 3 格
- **被动溃退**：士气 < 20（崩溃态）的单位在回合结束时必定溃退，失去玩家控制，AI 接管移向最近友方城市
- **追击机制**：步兵/骑兵自动追击离开 ZoC 的撤退/溃退单位，伤害 = 攻击力 × 0.3，无视防御
- 溃退到达友方城市后士气恢复到 30，恢复正常控制
- 被包围无路可走时每回合额外 -10 HP

**理由**：
- 主动撤退给玩家战术撤退选择，避免无意义的消耗战
- 被动溃退使崩溃态有行为后果，不仅是数值惩罚
- 追击机制惩罚鲁莽冒进，与 ZoC 配合形成前线压制效果

**已落地**：
- `docs/机制概览/战斗系统.md` → §6.4（崩溃状态效果更新）、§6.8 撤退与溃退
- `docs/策划决策/阶段5-平衡性调整文档.md` → 决策 #162
- `docs/策划决策/战斗系统设计缺口-程序反馈.md` → §2.2/§2.3 标记为已解决

---

## 待实现功能

- [ ] 回合顺序 UI 显示
- [ ] 势力行动提示
- [ ] 行动速度科技/建筑加成
- [ ] 特殊事件影响行动顺序
- [ ] ZoC 可视化（标记受控制格子）
- [ ] 撤退/溃退动画与路径显示
- [ ] 追击伤害数字弹出
